# 📋 تعليمات تنفيذ نظام المرتبات الكامل
## Complete Payroll System Implementation Guide

---

## 1️⃣ الجداول الجديدة في قاعدة البيانات

### ✅ تم إنشاء 3 SQL ملفات:

1. **add_hourly_rate_and_shifts.sql** ⏰
   - إضافة `hourly_rate` (سعر الساعة)
   - إضافة `shift_start_time` (وقت بداية الشيفت)
   - إضافة `shift_end_time` (وقت نهاية الشيفت)

2. **add_absences_and_deductions.sql** 📅
   - جدول `absences` (الغياب)
   - جدول `deductions` (الخصومات)
   - RLS Policies للمديرين

3. **add_payroll_system.sql** 💰
   - جدول `payroll_cycles` (دورات المرتبات)
   - جدول `employee_payrolls` (مرتبات الموظفين)
   - جدول `daily_attendance_summary` (ملخص الحضور اليومي)
   - Functions للحسابات

---

## 2️⃣ ترتيب التنفيذ (CRITICAL!)

### 🔴 يجب تنفيذ الملفات بهذا الترتيب:

```sql
-- الخطوة 1: إضافة سعر الساعة والشيفتات
\i add_hourly_rate_and_shifts.sql

-- الخطوة 2: إضافة نظام الغياب والخصومات
\i add_absences_and_deductions.sql

-- الخطوة 3: إضافة نظام المرتبات الكامل
\i add_payroll_system.sql
```

### ⚠️ ملاحظات مهمة:

- **لا تنفذ الملفات بترتيب عشوائي**
- افتح Supabase SQL Editor
- انسخ محتوى كل ملف ونفذه بالترتيب
- تأكد من عدم وجود أخطاء بعد كل خطوة

---

## 3️⃣ النظام الجديد

### 📊 هيكل الصفحات:

```
OwnerPayrollPage (صفحة المرتبات الرئيسية)
    ├── عرض جميع الفروع
    ├── إجمالي المرتبات لكل فرع
    ├── زر "تم الدفع" لكل فرع
    │
    ├── OwnerBranchPayrollDetailsPage (تفاصيل الفرع)
    │   ├── قائمة الموظفين
    │   ├── صافي المرتب لكل موظف
    │   ├── زر "دفع" لكل موظف
    │   │
    │   └── OwnerEmployeePayrollReportPage (التقرير التفصيلي)
    │       └── شيت كبير يحتوي على:
    │           ├── التاريخ
    │           ├── وقت الحضور
    │           ├── وقت الانصراف
    │           ├── عدد الساعات
    │           ├── المرتب اليومي
    │           ├── السلف
    │           ├── بدل الإجازة (100 ج.م)
    │           └── الخصومات
```

---

## 4️⃣ قواعد الحساب

### 💵 المرتب الأساسي:
```
المرتب الأساسي = عدد الساعات × سعر الساعة
```

### 🎁 بدل الإجازة:
```
IF (أيام الغياب > 0 AND أيام الغياب < 3):
    بدل الإجازة = 100 ج.م
ELSE:
    بدل الإجازة = 0
```

### 💸 صافي المرتب:
```
صافي المرتب = المرتب الأساسي + بدل الإجازة - السلف - الخصومات
```

### ⚠️ خصم الغياب (من نظام الغياب السابق):
```
خصم الغياب = 2 أيام × عدد ساعات الشيفت × سعر الساعة
```

---

## 5️⃣ ميزات النظام

### ✨ للمالك (Owner):

1. **صفحة المرتبات الرئيسية**:
   - ✅ عرض جميع الفروع
   - ✅ إجمالي المرتبات المستحقة لكل فرع
   - ✅ حالة الدفع (معلق/مدفوع)
   - ✅ زر "تم الدفع" مع تأكيد

2. **تفاصيل الفرع**:
   - ✅ قائمة الموظفين
   - ✅ صافي المرتب لكل موظف
   - ✅ تفاصيل: (ساعات، بدل إجازة، سلف، خصومات)
   - ✅ زر "دفع" لكل موظف
   - ✅ تحديث تلقائي للإجمالي عند دفع موظف

3. **التقرير التفصيلي للموظف**:
   - ✅ جدول كبير بكل أيام الفترة
   - ✅ وقت الحضور والانصراف
   - ✅ عدد الساعات اليومية
   - ✅ المرتب اليومي
   - ✅ السلف اليومية
   - ✅ بدل الإجازة
   - ✅ الخصومات
   - ✅ ملخص نهائي بالحسابات

---

## 6️⃣ التكامل التلقائي

### 🔄 عند تسجيل الحضور (Check-in):
```dart
1. تسجيل الحضور في جدول attendance
2. حفظ وقت الحضور في daily_attendance_summary
3. فحص التأخير (نظام الغياب)
4. تسجيل hourly_rate الحالي
```

### 🔄 عند تسجيل الانصراف (Check-out):
```dart
1. تسجيل الانصراف في جدول attendance
2. تحديث وقت الانصراف في daily_attendance_summary
3. حساب عدد الساعات
4. حساب المرتب اليومي (ساعات × hourly_rate)
```

---

## 7️⃣ الحقول المطلوبة في الداتابيز

### جدول `employees` (تحديثات):
- ✅ `hourly_rate` - سعر الساعة
- ✅ `shift_start_time` - وقت بداية الشيفت
- ✅ `shift_end_time` - وقت نهاية الشيفت

### جدول `payroll_cycles` (جديد):
- `id` - معرف الدورة
- `branch_id` - معرف الفرع
- `start_date` - تاريخ البداية
- `end_date` - تاريخ النهاية
- `total_amount` - إجمالي المرتبات
- `status` - الحالة (pending/paid)
- `paid_at` - وقت الدفع
- `paid_by` - من دفع

### جدول `employee_payrolls` (جديد):
- `id` - معرف السجل
- `payroll_cycle_id` - معرف الدورة
- `employee_id` - معرف الموظف
- `total_hours` - إجمالي الساعات
- `hourly_rate` - سعر الساعة
- `base_salary` - المرتب الأساسي
- `leave_allowance` - بدل الإجازة
- `total_advances` - إجمالي السلف
- `absence_days` - أيام الغياب
- `total_deductions` - إجمالي الخصومات
- `net_salary` - صافي المرتب
- `status` - الحالة (pending/paid)
- `paid_at` - وقت الدفع

### جدول `daily_attendance_summary` (جديد):
- `id` - معرف السجل
- `employee_id` - معرف الموظف
- `attendance_date` - التاريخ
- `check_in_time` - وقت الحضور
- `check_out_time` - وقت الانصراف
- `total_hours` - عدد الساعات
- `hourly_rate` - سعر الساعة
- `daily_salary` - المرتب اليومي
- `advance_amount` - السلفة
- `leave_allowance` - بدل الإجازة
- `deduction_amount` - الخصم
- `is_absent` - هل غائب
- `is_on_leave` - هل في إجازة

---

## 8️⃣ خطوات الاختبار

### 🧪 سيناريو الاختبار الكامل:

1. **إضافة موظف جديد**:
   - افتح صفحة الموظفين (Owner)
   - أضف موظف جديد
   - ✅ حدد سعر الساعة: 50 ج.م
   - ✅ حدد وقت الشيفت: 09:00 - 17:00 (8 ساعات)

2. **تسجيل حضور**:
   - سجل دخول بحساب الموظف
   - اضغط "تسجيل الحضور"
   - ✅ يجب حفظ البيانات في `daily_attendance_summary`

3. **تسجيل انصراف**:
   - اضغط "تسجيل الانصراف"
   - ✅ يجب حساب الساعات تلقائياً
   - ✅ يجب حساب المرتب اليومي

4. **عرض المرتبات**:
   - سجل دخول بحساب المالك
   - افتح تبويب "المرتبات"
   - ✅ يجب ظهور الفرع
   - ✅ يجب ظهور إجمالي المرتبات

5. **تفاصيل الفرع**:
   - اضغط على الفرع
   - ✅ يجب ظهور قائمة الموظفين
   - ✅ يجب ظهور صافي مرتب كل موظف

6. **التقرير التفصيلي**:
   - اضغط على موظف
   - ✅ يجب ظهور جدول بكل أيام الفترة
   - ✅ يجب ظهور وقت الحضور والانصراف
   - ✅ يجب ظهور عدد الساعات
   - ✅ يجب ظهور المرتب اليومي
   - ✅ يجب ظهور الإجمالي النهائي

7. **دفع المرتب**:
   - ارجع لتفاصيل الفرع
   - اضغط زر "دفع" للموظف
   - ✅ يجب تأكيد الدفع
   - ✅ يجب تحديث حالة الموظف إلى "مدفوع"
   - ✅ يجب خصم المبلغ من إجمالي الفرع

8. **دفع الفرع بالكامل**:
   - ارجع للصفحة الرئيسية
   - اضغط "تم الدفع" للفرع
   - ✅ يجب تأكيد الدفع
   - ✅ يجب تحديث حالة الفرع إلى "مدفوع"

---

## 9️⃣ الملفات المعدلة

### ✅ ملفات جديدة تم إنشاؤها:

1. `lib/services/payroll_service.dart` - خدمة المرتبات
2. `lib/screens/owner/owner_payroll_page.dart` - الصفحة الرئيسية
3. `lib/screens/owner/owner_branch_payroll_details_page.dart` - تفاصيل الفرع
4. `lib/screens/owner/owner_employee_payroll_report_page.dart` - التقرير التفصيلي

### ✅ ملفات تم تعديلها:

1. `lib/screens/employee/employee_home_page.dart` - تكامل check-in/out
2. `lib/screens/owner/owner_main_screen.dart` - إضافة تبويب المرتبات

---

## 🔟 حالات خاصة

### ❓ ماذا لو الموظف لم يسجل انصراف؟
- السيستم لن يحسب ساعات لهذا اليوم
- سيظهر في التقرير "لم يسجل انصراف"
- المالك يمكنه رؤية ذلك بوضوح

### ❓ ماذا لو الموظف غاب؟
- المدير يرفض الغياب من صفحة الغيابات
- يتم إنشاء سجل في `deductions`
- الخصم يظهر في صفحة المرتبات تلقائياً
- يطرح من صافي المرتب

### ❓ ماذا لو أخذ سلفة؟
- السلفة تسجل في `daily_attendance_summary`
- تظهر في التقرير التفصيلي
- تطرح من صافي المرتب

### ❓ بدل الإجازة متى يضاف؟
- إذا غاب الموظف أقل من 3 أيام في الشهر
- يضاف تلقائياً 100 ج.م
- يظهر في التقرير

---

## ✅ Checklist النهائي

قبل النشر، تأكد من:

- [ ] تنفيذ جميع ملفات SQL بالترتيب
- [ ] التأكد من عدم وجود أخطاء في قاعدة البيانات
- [ ] اختبار تسجيل حضور وانصراف
- [ ] التأكد من حفظ البيانات في `daily_attendance_summary`
- [ ] اختبار صفحة المرتبات (Owner)
- [ ] اختبار تفاصيل الفرع
- [ ] اختبار التقرير التفصيلي
- [ ] اختبار دفع موظف واحد
- [ ] اختبار دفع فرع كامل
- [ ] التأكد من تحديث الإجماليات تلقائياً
- [ ] اختبار بدل الإجازة (غياب أقل من 3 أيام)
- [ ] اختبار الخصومات (من نظام الغياب)
- [ ] اختبار السلف

---

## 📞 الدعم

في حالة وجود أي مشاكل:
1. تأكد من تنفيذ ملفات SQL بالترتيب الصحيح
2. افحص Logs في Supabase
3. تأكد من صلاحيات RLS
4. تأكد من وجود البيانات في الجداول

---

**تم بناء النظام بالكامل ✅**

نظام المرتبات جاهز للعمل بكفاءة عالية!
