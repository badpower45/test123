# تحديث نظام حساب بدل الإجازة - فترات الراتب الديناميكية

## 📋 التحديث الجديد

الـ edge function الآن تحسب **فترات الراتب تلقائياً** بناءً على تاريخ اليوم:

### فترات الراتب (Payroll Periods):

| الفترة | الأيام | الحالة |
|--------|--------|--------|
| **Period 1** | 1-15 | من أول الشهر إلى 15 |
| **Period 2** | 16-آخر | من 16 إلى نهاية الشهر |

---

## 🔄 آلية الحساب المحدثة

### 1. تحديد الفترة الحالية تلقائياً:
```typescript
const day = now.getDate();

if (day <= 15) {
  // نحن في فترة 1: من 1 إلى 15
  periodStart = "YYYY-MM-01"
  periodEnd = "YYYY-MM-15"
} else {
  // نحن في فترة 2: من 16 إلى آخر الشهر
  periodStart = "YYYY-MM-16"
  periodEnd = "YYYY-MM-31" (أو 30، 29، 28 حسب الشهر)
}
```

### 2. فحص طلبات الإجازة (مع تقاطع الفترات):
- الدالة تحضر **جميع** طلبات الإجازة المقبولة
- تفلترها لتشمل فقط الطلبات التي **تتقاطع** مع فترة الراتب الحالية
- مثال:
  - إذا طلب إجازة من 14-16، يحسب كـ overlapping لـ Period 1 (14-15 منه)
  - إذا طلب إجازة من 10-20، يحسب كـ overlapping للفترة الحالية

### 3. حساب البدل:
```
إذا overlapping leaves > 2 
  → allowance = 0 (بدون بدل)

إذا overlapping leaves == 0
  → allowance = (آخر يوم عمل قبل نهاية الفترة) × ساعات × سعر الساعة

إذا overlapping leaves == 1 أو 2
  → allowance = القيمة المحفوظة في جدول الموظفين
```

---

## 📊 أمثلة عملية

### مثال 1: نحن في 10 أبريل (Period 1)
```
الفترة: 1-15 أبريل
طلبات إجازة الموظف:
  - 5-7 أبريل ✓ (داخل الفترة)
  - 20-22 أبريل ✗ (خارج الفترة)
  
Overlapping = 1 طلب
Result: استخدام leave_allowance المحفوظة (100 مثلاً)
```

### مثال 2: نحن في 20 أبريل (Period 2)
```
الفترة: 16-30 أبريل
طلبات إجازة الموظف:
  - 5-7 أبريل ✗ (خارج الفترة)
  - 18-19 أبريل ✓ (داخل الفترة)
  - 25-26 أبريل ✓ (داخل الفترة)
  
Overlapping = 2 طلب
Result: استخدام leave_allowance المحفوظة (100)
```

### مثال 3: نحن في 12 أبريل (Period 1) بدون إجازات
```
الفترة: 1-15 أبريل
طلبات إجازة: صفر ✓

Overlapping = 0
Result: 
  آخر يوم عمل: 11 أبريل بـ 8 ساعات
  سعر الساعة: 100
  allowance = 8 × 100 = 800
```

---

## 🔧 التكامل مع Flutter

### لا يوجد تغيير في استدعاء الـ Function!

```dart
// نفس الكود في PayrollService:
double calculatedLeaveAllowance = await _payrollService.calculateLeaveAllowance(
  employeeId: widget.employeeId,
  // لا حاجة لـ month و year - تُحسب تلقائياً!
);
```

---

## 📝 استجابة الـ Edge Function

```json
{
  "success": true,
  "employee_id": "emp123",
  "period_start": "2026-04-01",
  "period_end": "2026-04-15",
  "period_name": "Period 1 (1-15)",
  "leave_request_count": 1,
  "leave_allowance": 100.0,
  "calculated_at": "2026-04-30T15:37:51.000Z"
}
```

---

## 🚀 الحالة

✅ Edge function محدثة ومنشورة على Supabase  
✅ Dart PayrollService لا يحتاج تغيير (تُحسب الفترات تلقائياً)  
✅ جاهز للإنتاج!

---

## 🧪 اختبار سريع

```bash
# 1. عند الدخول في يوم 1-15 من أي شهر:
# → سيحسب Period 1 (1-15)

# 2. عند الدخول في يوم 16+ من أي شهر:
# → سيحسب Period 2 (16-آخر الشهر)

# في كلا الحالتين، سيفحص الطلبات المتقاطعة بدقة
```
